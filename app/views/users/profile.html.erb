hello <%= spotify_user.email %>
<br>
<%= link_to 'Start a Party', '/parties/new', class:"btn btn-success btn-lg" %>

<h2>My Parties</h2>
<% if current_user.parties %>
  <ul>
    <% current_user.parties.each do |party| %>
      <li><%= link_to party.name, party_path(party) %></li>
    <% end %>
  </ul>
<% end %>
